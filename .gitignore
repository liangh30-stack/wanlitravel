node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
server/data/
*.bundle
*.command

# Logs de intercambio T10 (contienen datos de reservas — nunca al repo)
logs/

# SQLite auxiliares
server/data/*.db*

# Entregables de certificación generados (contienen datos de reservas reales)
Certificacion_*_WANLI.xlsx
